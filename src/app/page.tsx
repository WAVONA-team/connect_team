import React from "react";
import Container from "@/ui/container/Container";

const Home: React.FC = React.memo(() => {
  return (
    <Container>
      main
    </Container>
  );
});

export default Home;
